/*

n = int count
k = positive int
m = prime num

honorable = each is in s
all elem mul = k mod m
*/

#include <algorithm>
#include <bits/stdc++.h>
#include <climits>

using namespace std;

typedef long long ll;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);

  int n, k;
  ll m, l;
  cin >> n >> k >> m >> l;

  return 0;
}
